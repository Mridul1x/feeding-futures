export const metadata = {
  title: "Feeding Futures | Provider Connector Dashboard",
};

export default function ProviderConnectorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
